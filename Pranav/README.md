## Pranav Bhoj — Frontend (SPA) and Database Design

**Files:** `frontend/index.html`, `frontend/app.js` (partial), `schema.sql` (partial)

### Responsibilities:
- Built the entire **Vanilla JS Single-Page Application** — routing, state management, auth flow
- Designed and implemented all **UI pages** — Events grid, Seat map, My Tickets, Resale Marketplace, My Queue
- Implemented the **interactive seat selector** — click to select/deselect, sticky selection bar, max-4 cap
- Built the **booking modal** and confirmation flow
- Implemented **login/register forms** with tab switching and session persistence (`localStorage`)
- Added **toast notifications**, loading states, skeleton loaders, and responsive mobile navigation
